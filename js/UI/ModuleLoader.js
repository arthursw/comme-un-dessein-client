// Generated by CoffeeScript 1.10.0
(function() {
  define(['paper', 'R', 'Utils/Utils', 'UI/Button', 'UI/Code'], function(P, R, Utils, Button, Code) {
    var ModuleLoader, buttons;
    ModuleLoader = {};
    buttons = [
      {
        name: 'Precise path',
        description: "This path offers precise controls, one can modify points along with their handles and their type.",
        iconURL: 'icones_icon_pen.png',
        file: "Items/Paths/PrecisePaths/PrecisePath",
        category: void 0,
        favorite: true,
        order: 8
      }
    ];
    buttons[0].iconURL = R.style === 'line' ? 'icones_icon_pen.png' : R.style === 'romanesco' ? 'editCurve.png' : 'glyphicon-pencil';
    ModuleLoader.initialize = function() {
      var b, button, i, len;
      for (i = 0, len = buttons.length; i < len; i++) {
        button = buttons[i];
        b = new Button(button);
      }
    };
    ModuleLoader.load = function(name, callback) {
      var button, i, len;
      for (i = 0, len = buttons.length; i < len; i++) {
        button = buttons[i];
        if (button.name === name) {
          require([button.file], callback);
          return;
        }
      }
      console.log('unknown module: ' + name);
    };
    return ModuleLoader;
  });

}).call(this);
